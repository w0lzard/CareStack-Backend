server:
  port: ${PORT}

spring:
  profiles:
    default: local

  application:
    name: carestack

  datasource:
    url: ${DB_URL}
    username: ${DB_UNAME}
    password: ${DB_PSWD}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      # Flyway now owns the schema - Hibernate only checks entities match it,
      # it never creates or alters tables itself.
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: UTC

  flyway:
    enabled: true
    locations: ${MIGRATION_LOCATION}
    baseline-on-migrate: false

  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT}
      timeout: ${REDIS_TMEOUT}

  cache:
    type: redis

  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS}
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JacksonJsonSerializer
      acks: all
    consumer:
      group-id: carestack
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JacksonJsonDeserializer
      properties:
        spring.json.trusted.packages: "com.ryuken.carestack.event"
    listener:
      ack-mode: manual

hospital:
  scheduling:
    appointment-reminder-cron: "0 0 7 * * *"       # every day at 7 AM
    inventory-reconciliation-cron: "0 30 1 * * *"  # every day at 1:30 AM
  cache:
    default-ttl-minutes: 30
  idempotency:
    ttl-hours: 24

logging:
  level:
    root: INFO
    com.ryuken.carestack: DEBUG
    org.hibernate.SQL: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
